#include <stdio.h>
#include <stdbool.h>
#include <inttypes.h>

int is_prime(int n, int delitel);

int main() {
    int n = 0, delitel = 1;
    scanf("%d", &n);
    is_prime(n, delitel);
    return 0;
} 

int is_prime(int n, int delitel) {

    return 0;    
}